Top-level testing scripts are stored in the root of this dir.

Auto-generated testing scripts are then generated into sub-dirs.

To test the entirety of a given platform, drag the platform sub-dir (e.g. 'win32') onto the test_platform.bat script
